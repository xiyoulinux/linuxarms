#ifndef _LINUXARMS_H
#define _LINUXARMS_H

#include <string.h>
/* typedef a boolean type */
typedef enum _boolean {
	FALSE,
	TRUE
}boolean;

typedef enum _trans_state {
	STOP,
	CONTINUE,
}trans_state;
#endif
